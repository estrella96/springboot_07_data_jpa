spring:
  datasource:
    url: jdbc:mysql://localhost/User
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
#    更新或创建数据表
      ddl-auto: update
#      控制台显示sql
    show-sql: true